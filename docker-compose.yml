services:
  dna:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - .:/app
    environment:
      - PYTHONPATH=/app/backend
    command: uvicorn dna.api.app:app --host 0.0.0.0 --port 8000 --reload
